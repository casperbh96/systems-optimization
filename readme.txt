How to run:

1. Install Python 3.8 and Pip 3 (e.g. miniconda with `conda create -n envname python=3.8` or PyCharm)
2. Install requirements.txt using pip `pip install -r requirements.txt`
3. Run the main file, e.g. by a console command `python main.py`